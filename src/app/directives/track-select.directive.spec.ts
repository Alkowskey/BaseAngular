import { TrackSelectDirective } from './track-select.directive'

describe('TrackSelectDirective', () => {
  it('should create an instance', () => {
    const directive = new TrackSelectDirective()
    expect(directive).toBeTruthy()
  })
})
