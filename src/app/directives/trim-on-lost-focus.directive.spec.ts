import { TrimOnLostFocusDirective } from './trim-on-lost-focus.directive'

describe('TrimOnLostFocusDirective', () => {
  it('should create an instance', () => {
    const directive = new TrimOnLostFocusDirective()
    expect(directive).toBeTruthy()
  })
})
